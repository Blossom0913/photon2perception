# Photon2Perception requirements
# Python 3.8+

# Core
torch>=2.0.0
torchvision>=0.15.0

# RAW image loading
rawpy>=0.17.0
numpy>=1.21.0

# Training
pyyaml>=6.0
matplotlib>=3.5.0
scipy>=1.7.0

# Dataset loading: photon2perception/datasets/coco_raw_dataset.py
# (CocoRawDetectionDataset, standalone COCO-format loader used by
# tools/train.py's data.type='coco', without requiring mmdet/mmseg)
pycocotools>=2.0.6

# Optional: efficiency metrics
fvcore>=0.1.5
# thop>=0.1.1

# Model export / inference: tools/export.py, photon2perception/inference.py
onnx>=1.14.0
onnxruntime>=1.15.0

# Optional: OpenMMLab integration (for full dataset + eval support)
# mmdet>=3.0.0
# mmseg>=1.0.0

# Development
pytest>=7.0.0
